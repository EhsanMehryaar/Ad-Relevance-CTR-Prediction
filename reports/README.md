# Reports

This folder stores lightweight reporting artifacts that are safe to commit.

- `model_comparison.csv`: manually recorded comparison across trained models on a 200,000-row Criteo sample.

Large trained models and raw data are excluded from GitHub through `.gitignore`.
